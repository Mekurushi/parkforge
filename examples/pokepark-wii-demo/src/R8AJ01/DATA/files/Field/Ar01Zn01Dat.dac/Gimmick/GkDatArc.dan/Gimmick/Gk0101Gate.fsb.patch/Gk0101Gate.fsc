// Replaces the existing ACTIVATE function in Gk0101Gate.fsb.
// symbols.toml provides the original script's base address and the address of
// each function being replaced.
static void ACTIVATE(int object)
{
  int i = 1+1;
  return;
}
