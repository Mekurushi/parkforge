[package]
name = "parkforge-cli"
version.workspace = true
edition.workspace = true

[dependencies]
annotate-snippets = "0.12.16"
clap = { version = "4", features = ["derive"] }
parkforge = { path = "../parkforge" }
parkforge-types = { path = "../parkforge-types" }

[[bin]]
name = "parkforge"
path = "src/main.rs"

[lints]
workspace = true
